ALLOWED_ORIGIN=http://localhost:3000
DEFAULT_ANNUAL_RETURN_RATE=0.0
HABIT_COMPLETION_POINTS=10
GOAL_BONUS_POINTS=100
# Optional: enables LLM-narrated plans (see backend/ai_habits.py). Omit to
# use the deterministic offline template narrator instead.
GROQ_API_KEY=
